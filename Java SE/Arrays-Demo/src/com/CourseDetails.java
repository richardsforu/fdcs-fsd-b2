package com;

public class CourseDetails {

	/*
	 * This method will find all the courses whose duration is less than the given
	 * duration, and has number of vacant seats greater than 10 and return them in a
	 * new array as difference between maxCapacity and noOfLearners attribute. 
	 * Note: Number of vacant seats is computed
	 * 
	 */

	public static Course[] findCourses(Course[] courselist, int duration) {
		// fill the logic
		
				
		for(int i=0;i<courselist.length;i++) {
			
		}

		return null;
	}
	
	
	/*
	 * This method will update the fees of all courses whose no0fLearners has reached the 
	 * maxCapacity using the below formula
		newFees=OldFees-(10% of OldFees)
		The method will then return a string message like "Fees has been updated for x nunmber of Coursest
		Note: x should be replaced with number of courses whose fee got updated.
		 If no updates happened for any courses, x should take value 0
	 */

	public static String updateFees(Course course) {
		
		return null;
	}

}
